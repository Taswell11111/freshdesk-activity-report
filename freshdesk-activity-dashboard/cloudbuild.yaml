steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$freshdesk-activity-report/freshdesk-activity-dashboard-jan2026:$COMMIT_SHA', '.']
options:
  logging: CLOUD_LOGGING_ONLY
